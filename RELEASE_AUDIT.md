# Release Audit — zoom-monitor v1.0.0-lite

**Audit Date:** 2026-05-28  
**Build Hash:** `8c66c8371d6c491ac6cf7499781239154a5aeffa866d7d6739df76a4540ecf1a`  
**Tarball:** `dist/zoom-monitor-v1.0.0-lite.tar.gz` (702 KB, 63 files)  
**Auditor:** Hermes Agent (Automated)

---

## 1. Secrets Scan — 源码及文档

**Command:** `grep -RniE "sk-|AIza|xoxb-|TELEGRAM_BOT_TOKEN|CLIENT_SECRET|SECRET_KEY|ACCOUNT_ID|OPENAI_API_KEY|DEEPSEEK_API_KEY|SUB2API" /opt/zoom-monitor`

**Result: ✅ PASS**

All matches are **variable name references** or **documentation placeholders** only:
- `config.py`: `_env("ZOOM_ACCOUNT_ID")`, `_env("TELEGRAM_BOT_TOKEN")` — runtime reads from `.env`
- `README.md`, `docs/*.md`: `ZOOM_CLIENT_SECRET=***`, etc. — documentation with placeholder values
- `templates/*.html`: `{{ a.account_id[:20] }}` — Jinja2 template variable rendering
- No real API keys, tokens, or passwords found anywhere in source or docs

**Scanned directories excluded:** venv/, __pycache__/, dist/, old/, .git/, backups/, archive/, logs/

---

## 2. Tarball Content Check

**Command:** `tar -tzf dist/zoom-monitor-v1.0.0-lite.tar.gz`

**Result: ✅ PASS**

Banned files verified absent:
| Item | Status |
|------|--------|
| `.env` (real secrets) | ✅ Not present |
| `tracking.db` (production data) | ✅ Not present |
| `backups/` | ✅ Not present |
| `archive/` | ✅ Not present |
| `logs/` | ✅ Not present |
| `__pycache__/` | ✅ Not present |

**Note:** `backups/` directory exists on the server (contains old tracking.db backups) but is explicitly excluded from the release tarball by `build_release.sh`.

---

## 3. `.env.example` Placeholder Verification

**Command:** Inspected all key=value lines in `.env.example`

**Result: ✅ PASS**

All sensitive values are placeholders:
- `ZOOM_CLIENT_SECRET=***`
- `ZOOM_WEBHOOK_SECRET=***`
- `TELEGRAM_BOT_TOKEN=<truncated placeholder>`
- `SECRET_KEY=change...ring`
- `DEEPSEEK_API_KEY=<empty>`
- `ZOOM_PMI_ID=<empty>`
- All other values are safe defaults (0.0.0.0, numeric, sqlite paths, booleans)

One borderline value was sanitized during this audit:
- `DEEPSEEK_API_BASE=https://api.deepseek.com` → appended `# 替换为你的 API Base URL` comment

---

## 4. Demo Data Verification

**File:** `demo_data.py`

**Result: ✅ PASS**

- **24 fake names** — all clearly fictional (张三, 王五, 赵铁柱, 周星星, etc.)
- **All emails @example.com** — no real email addresses (zhang.san@example.com, test.a@example.com, etc.)
- **Meeting ID:** `"00000000000"` — clearly fake, with `# demo` comment
- **No real Telegram chat IDs** — confirmed via grep
- **No real Zoom meeting IDs (8033037476, 83595997615)** — confirmed via grep
- **No real person names (吴亦凡 etc.)** — previously patched to 吴大志; confirmed clean

---

## 5. `install.sh` Security Check

**File:** `install.sh` (254 lines)

**Result: ✅ PASS**

| Check | Status |
|-------|--------|
| No `chmod 777` | ✅ Only `chmod 600 .env` |
| No `eval` or `exec` on user input | ✅ Clean |
| No `curl | bash` on unknown scripts | ✅ Only uses `curl -fsSL https://get.docker.com | bash` (Docker official installer) |
| No secret exposure | ✅ Never reads `.env` content to stdout |
| No system file overwrite | ✅ Only writes to `/opt/zoom-monitor/` and `/etc/systemd/system/` via `sudo cp` |
| No unsafe `/tmp` writes | ✅ `/tmp/zoom-monitor-tmp` created then cleaned up with `rm -rf` |
| No hardcoded chat IDs | ✅ Confirmed via grep |
| Idempotent | ✅ .env existing detection, venv detection |

---

## 6. Overall Release Integrity

| Metric | Value |
|--------|-------|
| Build Hash (SHA256) | `8c66c8371d6c491ac6cf7499781239154a5aeffa866d7d6739df76a4540ecf1a` |
| File Count | 63 |
| Package Size | 702 KB |
| Release Date | 2026-05-28 |
| Zero real secrets | ✅ Confirmed |
| Zero banned files | ✅ Confirmed |
| Zero data leaks | ✅ Confirmed |

---

## Excluded Files (server-only, not in release)

The following files exist on the production server but are NOT included in the release tarball:

- `/opt/zoom-monitor/.env` — real secrets
- `/opt/zoom-monitor/data/tracking.db` — production database
- `/opt/zoom-monitor/backups/` — database backups
- `__pycache__/` — Python bytecode cache
- Any `.db` files, `.env` files, or `logs/` directory

---

## Audit Summary

**All 6 security checks PASSED.** The release tarball is clean and safe for public distribution.

**Action items for manual pre-launch:**
1. Regenerate Zoom Client Secret (Zoom Marketplace → App Credentials)
2. Regenerate Telegram Bot Token (BotFather → /newbot or /revoke)
3. Regenerate Zoom Webhook Secret (Zoom Marketplace → Feature → Webhook)
4. Update `.env` on server with new secrets

---

*Audit generated by Hermes Agent on 2026-05-28*
